---
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: py-jupyter
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: py-jupyter
    spec:
      containers:
      - name: pytorch
        image: prasanna1994/jupyter-notebook
        ports:
        - containerPort: 8888
        resources:
          requests:
            alpha.kubernetes.io/nvidia-gpu: {{.NvidiaGPU}}
            cpu: {{.CPU}}
          limits:
            alpha.kubernetes.io/nvidia-gpu: {{.NvidiaGPU}}
            cpu: {{.CPU}}
        volumeMounts:
        - name: nvidia-driver-384-111
          mountPath: /usr/local/nvidia
          readOnly: true
        - name: libcuda-so
          mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so
        - name: libcuda-so-1
          mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so.1
        - name: libcuda-so-384-111
          mountPath: /usr/lib/x86_64-linux-gnu/libcuda.so.384.111
          readOnly: true
      volumes:
      - name: nvidia-driver-384-111
        hostPath:
          path: /usr/lib/nvidia-384
      - name: libcuda-so
        hostPath:
          path: /usr/lib/x86_64-linux-gnu/libcuda.so
      - name: libcuda-so-1
        hostPath:
          path: /usr/lib/x86_64-linux-gnu/libcuda.so.1
      - name: libcuda-so-384-111
        hostPath:
          path: /usr/lib/x86_64-linux-gnu/libcuda.so.384.111
        
---
apiVersion: v1
kind: Service
metadata:
  name: py-jupyter-service
  labels:
    app: py-jupyter
spec:
  selector:
    app: py-jupyter
  ports:
  - port: 8888
    protocol: TCP
    nodePort: 30061
  type: LoadBalancer
---
